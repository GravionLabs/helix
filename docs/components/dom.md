# Dom

> Low-level DOM helper classes (connected-overlay scroll handling, DomHandler).

## Import

```ts
import { Dom } from '@helix-ui/core/dom';
```

## Classes

- `ConnectedOverlayScrollHandler`
- `DomHandler` — If delete below comment, we can see this error message: Metadata collected contains an error that will be reported at runtime: Only initialized variables and constants can be referenced because the value of this variable is needed by the template compiler.

## Functions

- `blockBodyScroll()`
- `unblockBodyScroll()`

## Source

[`projects/core/dom`](../../projects/core/dom)
