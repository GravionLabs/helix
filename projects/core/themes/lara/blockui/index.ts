import type { BlockUIDesignTokens, BlockUITokenSections } from '@gravionlabs/helix-core/themes/types';

export const root: BlockUITokenSections.Root = {
    borderRadius: '{content.border.radius}'
};

export default {
    root
} satisfies BlockUIDesignTokens;
