import type { SpeedDialDesignTokens, SpeedDialTokenSections } from '@gravionlabs/helix-core/themes/types';

export const root: SpeedDialTokenSections.Root = {
    gap: '0.5rem',
    transitionDuration: '{transition.duration}'
};

export default {
    root
} satisfies SpeedDialDesignTokens;
